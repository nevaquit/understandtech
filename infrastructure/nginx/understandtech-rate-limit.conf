# Rate limiting zones — include from http {} context in nginx.conf:
#   include /etc/nginx/conf.d/understandtech-rate-limit.conf;

# 5 requests/minute per IP on Moodle login (playbook requirement).
limit_req_zone $binary_remote_addr zone=moodle_login:10m rate=5r/m;
