# block_examreadiness

Dashboard block showing exam readiness %, domain radar data, and dangerous misconceptions via `local_certmaster` API.

## Configuration

Add block to My Moodle; set **Certification ID** (Security+ seeds as id `1` on fresh install).
