define([ 'jquery', 'core/templates', 'core/log', 'core_filters/events', ], function($, Templates, log, filterEvents) { 'use strict'; if (Templates.__utDomPatchApplied) { return {}; } const canConstructYuiNodeList = () => { if (typeof Y === 'undefined' || !Y.NodeList) { return false; } try { new Y.NodeList([]); return true; } catch (error) { return false; } }; const safeYuiDestroy = (nodes) => { if (!nodes || !nodes.length) { return; } try { if (typeof Y !== 'undefined' && typeof Y.all === 'function') { Y.all(nodes).destroy(true); return; } if (canConstructYuiNodeList()) { new Y.NodeList(nodes).destroy(true); } } catch (error) { log.debug('theme_understandtech/templates_dom_patch: skipped YUI destroy'); } }; const domReplaceSafe = (element, newHTML, newJS, replaceChildNodes) => { const replaceNode = $(element); if (!replaceNode.length) { return []; } const newNodes = $(newHTML); if (replaceChildNodes) { safeYuiDestroy(replaceNode.children().get()); replaceNode.empty(); replaceNode.append(newNodes); } else { safeYuiDestroy(replaceNode.get()); replaceNode.replaceWith(newNodes); } if (newJS && typeof Templates.runTemplateJS === 'function') { Templates.runTemplateJS(newJS); } if (filterEvents && typeof filterEvents.notifyFilterContentUpdated === 'function') { filterEvents.notifyFilterContentUpdated(newNodes); } return newNodes.get(); }; const patchMethod = (methodName, replaceChildNodes) => { const original = Templates[methodName]; if (typeof original !== 'function') { return; } Templates[methodName] = function(element, newHTML, newJS) { if (!canConstructYuiNodeList()) { return domReplaceSafe(element, newHTML, newJS, replaceChildNodes); } try { return original.call(Templates, element, newHTML, newJS); } catch (error) { const message = String(error); if (message.includes('NodeList') || message.includes('Y.')) { log.debug('theme_understandtech/templates_dom_patch: fallback for ' + methodName); return domReplaceSafe(element, newHTML, newJS, replaceChildNodes); } throw error; } }; }; if (typeof Y !== 'undefined' && typeof Y.use === 'function') { Y.use('node', function() {}); } patchMethod('replaceNode', false); patchMethod('replaceNodeContents', true); Templates.__utDomPatchApplied = true; log.debug('theme_understandtech/templates_dom_patch: applied'); return {}; });